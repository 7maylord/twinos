{
  "buildCommand": "npx prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/check-stale-predictions",
      "schedule": "0 9 * * *"
    }
  ]
}
